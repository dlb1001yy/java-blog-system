# 部署操作手册

> 本文档涵盖从镜像拉取到访问测试的完整部署流程，适用于 Ubuntu 22.04 LTS 虚拟机环境。
>
> 前置条件：Docker 与 Docker Compose 已安装。如未安装，请先参考 [Docker环境安装指南.md](Docker环境安装指南.md)。

---

## 目录

1. [部署前准备](#1-部署前准备)
2. [Docker 镜像加速器配置](#2-docker-镜像加速器配置)
3. [基础镜像预拉取](#3-基础镜像预拉取)
4. [一键启动全部服务](#4-一键启动全部服务)
5. [（可选）启用 Elasticsearch 全文检索](#5-可选启用-elasticsearch-全文检索)
6. [逐服务状态检查](#6-逐服务状态检查)
7. [访问测试](#7-访问测试)
8. [常见部署问题排查](#8-常见部署问题排查)
9. [停止与重新部署](#9-停止与重新部署)
10. [（可选）监控体系部署与使用](#10-可选监控体系部署与使用)

---

## 1. 部署前准备

### 1.1 确认 Docker 已安装

```bash
docker --version
# 预期输出：Docker version 24.x.x 或更高

docker compose version
# 预期输出：Docker Compose version v2.x.x
```

### 1.2 确认代码已克隆

```bash
cd ~/java-blog-system
ls -la
```

预期看到以下关键文件：

```
docker-compose.yml
blog-backend/
blog-admin/
blog-frontend/
```

### 1.3 检查 Docker 相关文件

```bash
# 确认 Dockerfile 存在
ls blog-backend/Dockerfile blog-admin/Dockerfile blog-frontend/Dockerfile

# 确认 nginx 配置存在
ls blog-admin/nginx.conf blog-frontend/nginx.conf

# 确认 docker 配置存在
ls blog-backend/src/main/resources/application-docker.yaml

# 确认数据库初始化脚本存在
ls blog-backend/sql/01-create_sql.sql

# 确认 Elasticsearch 构建文件存在（可选全文检索服务）
ls elasticsearch/Dockerfile
```

### 1.4 查看服务器 IP

```bash
ip addr show | grep 'inet ' | grep -v '127.0.0.1'
# 记下虚拟机 IP，后续访问需要用到
```

### 1.5 环境变量与 .env 配置（首次部署必读）

项目的敏感配置（数据库/Redis 密码、JWT 密钥、API 签名密钥等）统一通过环境变量注入：`docker-compose.yml` 与后端 `application-docker.yaml` 均使用 `${VAR:-默认值}` 占位。根目录提供 `.env.example` 模板，`.env` 已被 `.gitignore` 忽略，不会提交到 git。

**变量一览**（带 ★ 的项生产环境必须修改）：

| 变量 | 默认值 | 说明 |
|------|--------|------|
| COMPOSE_PROFILES | — | Compose 服务分组（`search` = 附带 Elasticsearch 全文检索；`monitor` = 附带 Prometheus + Grafana 监控；可组合如 `search,monitor`） |
| MYSQL_ROOT_PASSWORD ★ | 123456 | MySQL root 密码（容器初始化 + 后端连接 + 健康检查共用） |
| MYSQL_USERNAME | root | 后端连接 MySQL 的账号 |
| MYSQL_PASSWORD | 123456 | 后端连接 MySQL 的密码（须与 root 密码保持一致） |
| REDIS_PASSWORD ★ | 123456 | Redis 密码 |
| JWT_SECRET ★ | 内置开发默认值 | JWT 签名密钥（≥64 字符，强随机） |
| API_SIGNING_SECRET ★ | 内置开发默认值 | API 请求签名密钥（须与前端构建密钥一致） |
| BLOG_SEARCH_ENABLED | false（模板预设 true） | 是否启用 Elasticsearch 全文检索（true 需 COMPOSE_PROFILES 包含 search） |
| GRAFANA_ADMIN_USER ★ | admin | Grafana 管理员用户名（启用 monitor 分组时生效） |
| GRAFANA_ADMIN_PASSWORD ★ | admin123 | Grafana 管理员登录密码（启用 monitor 分组时生效，生产必改） |
| OSS_ENDPOINT | 占位符 | OSS 对象存储 Endpoint（启用 oss 存储时配置） |
| OSS_ACCESS_KEY_ID | 占位符 | OSS AccessKey ID |
| OSS_ACCESS_KEY_SECRET | 占位符 | OSS AccessKey Secret |
| OSS_BUCKET_NAME | 占位符 | OSS Bucket 名称 |
| OSS_URL_PREFIX | 占位符 | OSS 文件访问 URL 前缀 |

**操作步骤**：

1. 在项目根目录复制模板并编辑：

```bash
cp .env.example .env
vim .env   # 按需修改，重点核对带 ★ 的项
```

2. 启动服务（`.env` 中的变量会自动注入 docker-compose.yml 与后端容器）：

```bash
docker compose up -d --build
```

**重要说明**：

- **JWT_SECRET**：长度必须 ≥ 64 字符（HS512 要求），生产环境务必替换为强随机值，可用 `openssl rand -base64 64` 生成。
- **API_SIGNING_SECRET**：须与前端构建时的签名密钥一致，否则签名校验失败、管理后台无法登录：
  - blog-admin：构建时环境变量 `VITE_API_SIGNING_SECRET`，或 `blog-admin/.env.production` 中的取值
  - blog-app：发行前修改 `blog-app/common/env.js` 中的 `SIGNING_SECRET`
- **改密不回溯**：`MYSQL_ROOT_PASSWORD` 仅在 MySQL 数据卷**首次初始化**时生效。若数据卷已用旧密码初始化，事后修改 `.env` 中的密码不会同步进数据库（后端会因密码不符而连接失败），需二选一处理：
  - 进容器手动改密（保留数据）：

    ```bash
    docker exec blog-mysql mysql -uroot -p<旧密码> -e "ALTER USER 'root'@'%' IDENTIFIED BY '<新密码>'; FLUSH PRIVILEGES;"
    ```

  - 删除数据卷重建（⚠️ 清空全部数据）：`docker compose down -v` 后再重新启动

---

## 2. Docker 镜像加速器配置

国内网络环境拉取 Docker Hub 镜像可能超时或被限流（429 Too Many Requests），**必须配置镜像加速器**。

### 2.1 查看当前配置

```bash
cat /etc/docker/daemon.json
```

### 2.2 配置镜像加速器

推荐使用项目内一键脚本（自动备份+合并+重启+验证）：`sudo bash scripts/configure-docker-mirror.sh`

```bash
sudo tee /etc/docker/daemon.json <<'EOF'
{
  "registry-mirrors": [
    "https://docker.xuanyuan.me",
    "https://docker.1ms.run",
    "https://docker.m.daocloud.io",
    "https://docker.1panel.live",
    "https://dockerproxy.link"
  ],
  "log-driver": "json-file",
  "log-opts": {
    "max-size": "100m",
    "max-file": "3"
  }
}
EOF
```

### 2.3 重启 Docker 生效

```bash
sudo systemctl daemon-reload
sudo systemctl restart docker
```

### 2.4 验证镜像加速

```bash
docker info | grep -A 5 "Registry Mirrors"
```

### 2.5 （推荐）配置阿里云专属加速器

公共镜像加速器可能被限流，阿里云提供免费的个人专属加速器，稳定性最好：

1. 访问 https://cr.console.aliyun.com/cn-hangzhou/instances/mirrors
2. 使用阿里云账号登录
3. 页面会显示你的专属加速器地址，格式类似：`https://xxxxxx.mirror.aliyuncs.com`
4. 替换 daemon.json：

```bash
sudo tee /etc/docker/daemon.json <<'EOF'
{
  "registry-mirrors": [
    "https://xxxxxx.mirror.aliyuncs.com",
    "https://docker.m.daocloud.io",
    "https://docker.1ms.run"
  ],
  "log-driver": "json-file",
  "log-opts": {
    "max-size": "100m",
    "max-file": "3"
  }
}
EOF

sudo systemctl daemon-reload
sudo systemctl restart docker
```

> 将 `xxxxxx` 替换为你的阿里云加速器 ID。

---

## 3. 基础镜像预拉取

在执行 `docker compose up` 之前，建议先单独拉取所有基础镜像。这样可以避免构建时因镜像拉取超时而失败，也便于排查网络问题。

### 3.1 需要拉取的镜像列表

按服务依赖顺序排列：

| 序号 | 镜像名称 | 用途 | 大小参考 | 拉取命令 |
|------|----------|------|----------|----------|
| 1 | `mysql:8.0` | 数据库 | ~600MB | `docker pull mysql:8.0` |
| 2 | `redis:7-alpine` | 缓存 | ~40MB | `docker pull redis:7-alpine` |
| 3 | `eclipse-temurin:17-jdk-jammy` | 后端构建阶段 | ~350MB | `docker pull eclipse-temurin:17-jdk-jammy` |
| 4 | `eclipse-temurin:17-jre-jammy` | 后端运行阶段 | ~250MB | `docker pull eclipse-temurin:17-jre-jammy` |
| 5 | `node:18-alpine` | 前端构建阶段 | ~150MB | `docker pull node:18-alpine` |
| 6 | `nginx:1.25-alpine` | 前端运行阶段 | ~50MB | `nginx:1.25-alpine` |

> **说明**：`eclipse-temurin:17-jdk-jammy` 与 `eclipse-temurin:17-jre-jammy` 共享 Ubuntu jammy 基础层，拉取第一个后第二个会快很多。

> **关于 Elasticsearch（可选）**：Elasticsearch 全文检索服务（`elasticsearch`）默认**不随主命令启动**，它通过项目内 `elasticsearch/Dockerfile` 本地构建生成镜像 `blog-elasticsearch:8.11.1-ik`（基于 `docker.elastic.co/elasticsearch/elasticsearch:8.11.1` + IK 中文分词插件）。该基础镜像会在你执行 `docker compose --profile search up -d --build` 时自动拉取，**无需手动 `docker pull blog-elasticsearch`**。如需预拉取该基础镜像，可执行：
>
> ```bash
> docker pull docker.elastic.co/elasticsearch/elasticsearch:8.11.1
> ```

### 3.2 逐个拉取镜像

```bash
# 1. 拉取 MySQL（最先需要）
docker pull mysql:8.0

# 2. 拉取 Redis
docker pull redis:7-alpine

# 3. 拉取后端构建镜像（JDK，较大）
docker pull eclipse-temurin:17-jdk-jammy

# 4. 拉取后端运行镜像（JRE，与 JDK 共享层，很快）
docker pull eclipse-temurin:17-jre-jammy

# 5. 拉取前端构建镜像
docker pull node:18-alpine

# 6. 拉取前端运行镜像
docker pull nginx:1.25-alpine
```

### 3.3 验证镜像已拉取

```bash
docker images | grep -E 'mysql|redis|eclipse-temurin|node|nginx'
```

预期输出包含以上 6 个镜像。

### 3.4 如果拉取超时或 429

```bash
# 查看拉取错误信息
docker pull mysql:8.0 2>&1 | tail -5

# 解决方案：
# 1. 确认镜像加速器已配置（见第 2 节）
# 2. 使用阿里云专属加速器（见第 2.5 节）
# 3. 重试拉取
docker pull mysql:8.0
```

### 3.5 离线方式（服务器无外网/下载慢）

如果服务器直接拉取镜像仍然很慢，可在另一台有外网的 Linux/macOS 机器上提前导出镜像：

1. 外网机器（已装 Docker）在项目目录执行：`bash scripts/export-images.sh`，生成 `images/` 目录。
2. 将 `images/` 目录拷贝到服务器项目根目录（scp / U盘）。
3. 服务器执行：`sudo bash scripts/load-images.sh`，逐个 `docker load`。
4. 然后正常执行：`docker compose up -d --build`，此时不再从 Docker Hub 拉取这些基镜像。

---

## 4. 一键启动全部服务

### 4.1 执行启动命令

```bash
cd ~/java-blog-system
docker compose up -d --build
```

- `-d`：后台运行
- `--build`：构建镜像（首次必须加）

### 4.2 启动顺序说明

Docker Compose 按依赖顺序启动服务：

```
MySQL（启动 + 健康检查通过）
    ↓
Redis（启动 + 健康检查通过）
    ↓
blog-backend（Maven 编译 → JAR 打包 → 启动 → 健康检查通过）
    ↓
blog-admin（npm 编译 → nginx 启动）
blog-frontend（npm 编译 → nginx 启动）
```

### 4.3 启动进度查看

```bash
# 查看全部服务实时日志
docker compose logs -f

# 单独查看后端构建与启动日志（最耗时的部分）
docker compose logs -f blog-backend

# 当后端日志出现以下字样时，说明后端已启动成功：
# "Started JavaBlogApplication in x.xxx seconds"
```

### 4.4 首次构建耗时参考

| 阶段 | 预计耗时 | 说明 |
|------|----------|------|
| MySQL + Redis 启动 | 30-60s | 基础服务就绪 |
| 后端 Maven 依赖下载 | 3-10min | 首次需下载所有依赖（走阿里云 Maven 镜像） |
| 后端编译打包 | 1-2min | Maven 编译 + JAR 打包 |
| 后端启动 | 30-60s | Spring Boot 启动 |
| 管理后台 npm install + build | 2-5min | 首次需下载 npm 依赖 |
| 前台门户 npm install + build | 2-5min | 首次需下载 npm 依赖 |
| **总计** | **8-20min** | 取决于网络与服务器性能 |

> 后续重新构建会利用 Docker 缓存层，耗时大幅减少。

---

## 5. （可选）启用 Elasticsearch 全文检索

Elasticsearch 提供文章中文全文检索能力（基于 `elasticsearch:8.11.1` + IK 中文分词插件）。该服务在 `docker-compose.yml` 中通过 `profiles: ["search"]` 定义为**可选服务**，默认不会随 `docker compose up` 启动。只有在需要全文检索功能时才启用。

### 5.1 启用方式（双条件）

启用 Elasticsearch 必须同时满足以下两个条件，缺一不可：

**条件一：启动时指定 `search` profile**

```bash
cd ~/java-blog-system
docker compose --profile search up -d --build
```

> 执行该命令时，Docker Compose 会构建 `blog-elasticsearch:8.11.1-ik` 镜像（基础镜像 `docker.elastic.co/elasticsearch/elasticsearch:8.11.1` 会自动拉取），并启动 `elasticsearch` 服务。

**条件二：开启后端检索开关**

后端通过环境变量 `BLOG_SEARCH_ENABLED` 控制是否启用检索（默认 `false`，即不启用；该变量的完整说明已并入 1.5 节变量表，此处仅列开启方式）。在 `blog-backend` 服务中该变量默认关闭，需显式开启：

```bash
# 临时生效（仅当前 shell，配合启动命令）
export BLOG_SEARCH_ENABLED=true
docker compose --profile search up -d --build blog-backend

# 或在 .env 文件中持久配置（推荐）
echo "BLOG_SEARCH_ENABLED=true" >> .env
docker compose --profile search up -d --build
```

> 如果只启动 `elasticsearch` 容器而未设置 `BLOG_SEARCH_ENABLED=true`，后端不会连接 ES，全文检索功能不生效（文章仍可正常发布与浏览，只是搜索走的是数据库模糊查询）。

### 5.2 Elasticsearch 关键配置

来自 `docker-compose.yml` 与 `application-docker.yaml` 的实际配置：

| 配置项 | 值 | 说明 |
|--------|-----|------|
| 容器名 | `blog-elasticsearch` | 后端通过主机名 `elasticsearch` 访问 |
| 镜像 | `blog-elasticsearch:8.11.1-ik`（本地构建） | 基于 ES 8.11.1 + IK 分词 |
| 端口 | `9200`（HTTP）、`9300`（Transport） | 对外暴露 9200 |
|  discovery.type | `single-node` | 单节点模式 |
| xpack.security.enabled | `false` | 关闭安全认证 |
| ES_JAVA_OPTS | `-Xms512m -Xmx512m` | JVM 堆内存，默认 512MB |
| 后端连接地址 | `http://elasticsearch:9200` | `spring.elasticsearch.uris` |
| 数据卷 | `blog_es_data` | 索引数据持久化 |

> **内存注意**：ES 对内存较敏感。若宿主机内存紧张（建议 ≥ 2GB 可用），可适当调大 `ES_JAVA_OPTS`，但不超过宿主机物理内存的 50%。

### 5.3 不启用 Elasticsearch 的情况

若不需要全文检索，直接使用主命令启动即可，无需任何额外操作：

```bash
docker compose up -d --build
```

此时 `elasticsearch` 服务不会启动，后端 `BLOG_SEARCH_ENABLED` 默认为 `false`，系统正常运行。

---

## 6. 逐服务状态检查

### 6.1 查看全部服务状态

```bash
docker compose ps
```

预期输出（全部为 `running` 或 `healthy`）：

```
NAME              IMAGE                              STATUS                    PORTS
blog-admin        blog-admin                         Up X minutes              0.0.0.0:8081->80/tcp
blog-frontend     blog-frontend                      Up X minutes              0.0.0.0:8082->80/tcp
blog-backend      blog-backend                       Up X minutes (healthy)    0.0.0.0:8080->8080/tcp
blog-mysql        mysql:8.0                          Up X minutes (healthy)    0.0.0.0:3306->3306/tcp
blog-redis        redis:7-alpine                     Up X minutes (healthy)    0.0.0.0:6379->6379/tcp
```

### 6.2 健康检查说明

| 服务 | 健康检查方式 | start_period |
|------|-------------|--------------|
| MySQL | `mysqladmin ping` | 30s |
| Redis | `redis-cli ping` | 10s |
| blog-backend | `curl http://localhost:8080/api/portal/stats` | 60s |
| elasticsearch（可选） | `curl -s http://localhost:9200/_cluster/health` 检查 green/yellow | 60s |

> `blog-backend` 的健康检查 `start_period` 为 60 秒，在此期间即使检查失败也不会标记为 unhealthy。
>
> `elasticsearch` 仅在启用 `search` profile 时出现，其健康检查检查集群状态为 `green` 或 `yellow` 即视为健康。

### 6.3 查看各服务日志

```bash
# 查看后端日志（排查启动失败）
docker compose logs -f blog-backend

# 查看 MySQL 日志
docker compose logs -f mysql

# 查看 Redis 日志
docker compose logs -f redis

# 查看 Elasticsearch 日志（启用 search profile 后）
docker compose logs -f elasticsearch

# 查看管理后台日志
docker compose logs -f blog-admin

# 查看前台门户日志
docker compose logs -f blog-frontend
```

### 6.4 检查容器资源占用

```bash
docker stats --no-stream
```

---

## 7. 访问测试

将下文中的 `<服务器IP>` 替换为实际 IP（本机部署使用 `localhost`，使用 `ip addr` 查看）。

### 7.1 测试后端 API

```bash
# 测试后端是否正常响应
curl http://localhost:8080/api/portal/stats

# 预期返回 JSON 数据，包含 articleCount、viewCount、categoryCount 等字段
```

### 7.2 测试接口文档

浏览器访问：

```
http://<服务器IP>:8080/api/doc.html
```

预期显示 Knife4j 接口文档页面。

### 7.3 测试前台门户

浏览器访问：

```
http://<服务器IP>:8082/blog/
```

> **注意**：路径必须包含 `/blog/` 子路径，访问根路径会自动重定向。

预期显示博客前台首页，能看到文章列表、统计数据等。

### 7.4 测试管理后台

浏览器访问：

```
http://<服务器IP>:8081/admin/
```

> **注意**：路径必须包含 `/admin/`（含尾部斜杠）。

预期显示管理后台登录页面。使用默认账号登录：

| 用户名 | 密码 |
|--------|------|
| admin | admin123 |

登录后预期看到 Dashboard 仪表盘页面。

### 7.5 测试上传功能

在管理后台编辑文章时上传图片，确认图片可正常上传并显示：

```bash
# 检查上传目录是否有文件
docker exec blog-backend ls -la /app/uploads/

# 检查上传文件可访问
curl -I http://localhost:8080/api/uploads/
```

### 7.6 测试 Elasticsearch（仅启用 search profile 后）

```bash
# 测试 ES 是否可访问（返回集群信息 JSON 即正常）
curl http://localhost:9200

# 预期返回包含 "cluster_name"、"version"（number 为 8.11.1）等字段的 JSON

# 查看集群健康状态
curl http://localhost:9200/_cluster/health?pretty
# 预期 status 为 "green" 或 "yellow"

# （启用检索后）查看文章索引是否存在
curl http://localhost:9200/_cat/indices?v
# 启用 BLOG_SEARCH_ENABLED=true 且文章发布后，应能看到 blog_article 相关索引
```

### 7.7 访问地址汇总

| 服务 | 地址 | 说明 |
|------|------|------|
| 前台门户 | http://\<服务器IP\>:8082/blog/ | 博客前台（路径含 /blog/） |
| 管理后台 | http://\<服务器IP\>:8081/admin/ | 管理后台（路径含 /admin/） |
| 后端 API | http://\<服务器IP\>:8080/api | 后端接口 |
| 接口文档 | http://\<服务器IP\>:8080/api/doc.html | Knife4j 文档 |
| Elasticsearch（可选） | http://\<服务器IP\>:9200 | 全文检索（启用 search profile 后） |

如配置了外部 Nginx（80 端口）统一入口，也可通过以下地址访问：

| 服务 | 地址 |
|------|------|
| 前台门户 | http://\<服务器IP\>/blog/ |
| 管理后台 | http://\<服务器IP\>/admin/ |

---

## 8. 常见部署问题排查

### 8.1 镜像拉取 429 Too Many Requests

**现象**：

```
failed to resolve source metadata for docker.io/library/xxx: unexpected status from GET request: 429 Too Many Requests
```

**原因**：公共镜像加速器被限流。

**解决**：

```bash
# 1. 配置阿里云专属加速器（见第 2.5 节）
# 2. 重启 Docker
sudo systemctl restart docker
# 3. 重试拉取
docker pull <镜像名>
```

### 8.2 后端构建 apt-get 超时

**现象**：`docker compose up --build` 卡在 `RUN apt-get update` 超过 5 分钟。

**原因**：容器内 `apt-get` 从 `archive.ubuntu.com` 下载缓慢。

**解决**：当前 Dockerfile 已移除 `apt-get`，使用 Docker `ADD` 指令直接下载 Maven。如仍遇到此问题，确认使用的是最新 Dockerfile：

```bash
# 检查 Dockerfile 是否包含 ADD 指令（而非 apt-get）
grep "ADD" blog-backend/Dockerfile
```

### 8.3 Maven 下载慢 / 构建卡住

**现象**：后端构建卡在 `mvn dependency:go-offline` 或 `mvn package` 超过 10 分钟。

**原因**：Maven 从中央仓库下载依赖缓慢。

**解决**：当前 Dockerfile 已配置阿里云 Maven 镜像。确认配置存在：

```bash
grep "aliyun" blog-backend/Dockerfile
# 预期看到阿里云 Maven 镜像 URL
```

如仍慢，可手动在宿主机预编译 JAR 后修改 Dockerfile 直接复制：

```bash
# 在宿主机（需有 JDK + Maven）
cd blog-backend
mvn clean package -DskipTests
# 然后修改 Dockerfile 跳过编译阶段，直接 COPY target/*.jar
```

### 8.4 端口冲突

**现象**：容器启动报错 `bind: address already in use`。

**解决**：

```bash
# 查看占用端口的进程
sudo lsof -i :8080
sudo lsof -i :8081
sudo lsof -i :8082

# 停止占用进程
sudo kill -9 <PID>

# 或修改 docker-compose.yml 中的端口映射
# 例如将 "8080:8080" 改为 "9090:8080"
```

### 8.5 数据库连接失败

**现象**：后端日志报错 `Communications link failure` 或 `Access denied for user 'root'`。

**解决**：

```bash
# 1. 确认 MySQL 容器已健康启动
docker compose ps mysql
# 状态应为 healthy

# 2. 检查后端配置中的数据库连接信息
cat blog-backend/src/main/resources/application-docker.yaml | grep -A 5 datasource
# host 应为 mysql，password 应为 123456

# 3. 手动测试数据库连接
docker exec blog-mysql mysql -uroot -p123456 -e "SHOW DATABASES;"
# 应看到 dlbyy_zp_blog 数据库

# 4. 如数据库未初始化，手动执行 SQL
docker exec -i blog-mysql mysql -uroot -p123456 dlbyy_zp_blog < blog-backend/sql/01-create_sql.sql
```

### 8.6 前端页面空白 / 404

**现象**：访问管理后台或前台门户显示空白页或 404。

**解决**：

```bash
# 1. 确认后端健康检查通过（前端依赖后端）
docker compose ps blog-backend
# 状态应为 healthy

# 2. 管理后台必须访问 /admin/ 路径（含尾部斜杠）
# 正确：http://<IP>:8081/admin/
# 错误：http://<IP>:8081/

# 3. 前台门户必须访问 /blog/ 路径（含子路径）
# 正确：http://<IP>:8082/blog/
# 错误：http://<IP>:8082/

# 4. 检查 nginx 配置
docker exec blog-admin cat /etc/nginx/conf.d/default.conf
docker exec blog-frontend cat /etc/nginx/conf.d/default.conf

# 5. 检查前端构建产物是否存在
# 管理后台产物在 html/admin/ 子目录
docker exec blog-admin ls /usr/share/nginx/html/admin/
# 前台门户产物在 html/blog/ 子目录
docker exec blog-frontend ls /usr/share/nginx/html/blog/
```

### 8.7 后端健康检查不通过

**现象**：`docker compose ps` 显示 blog-backend 状态为 `unhealthy`。

**解决**：

```bash
# 查看后端日志
docker compose logs --tail 50 blog-backend

# 常见原因：
# - 数据库未就绪：等待 MySQL healthy 后重启后端
docker compose restart blog-backend

# - 应用启动报错：查看日志中的异常堆栈
# - 健康检查接口无响应：手动测试
docker exec blog-backend curl http://localhost:8080/api/portal/stats
```

### 8.8 npm install 慢

**现象**：前端构建卡在 `npm install` 超过 10 分钟。

**解决**：当前 Dockerfile 已配置 npmmirror 镜像源。如仍慢：

```bash
# 确认 Dockerfile 中有 --registry 参数
grep "registry" blog-admin/Dockerfile blog-frontend/Dockerfile
# 预期看到 https://registry.npmmirror.com
```

### 8.9 Elasticsearch 相关排查

#### 8.9.1 ES 容器未启动 / 找不到服务

**现象**：执行 `docker compose ps` 看不到 `blog-elasticsearch`；或 `curl http://localhost:9200` 连接失败。

**原因**：Elasticsearch 是可选服务（`profiles: ["search"]`），默认不随 `docker compose up` 启动。

**解决**：必须带 `--profile search` 启动：

```bash
docker compose --profile search up -d --build
docker compose ps elasticsearch
# 应能看到 blog-elasticsearch 为 Up / healthy
```

#### 8.9.2 全文检索不生效（开关未开）

**现象**：ES 容器已启动且健康，但博客前台/后台搜索仍走数据库模糊查询，或后台报 ES 连接相关警告。

**原因**：后端通过环境变量 `BLOG_SEARCH_ENABLED` 控制检索开关，默认为 `false`，未开启时后端不连接 ES。

**解决**：开启开关并重启后端：

```bash
# 方式一：写入 .env 持久生效
echo "BLOG_SEARCH_ENABLED=true" >> .env
docker compose --profile search up -d --build

# 方式二：临时环境变量
export BLOG_SEARCH_ENABLED=true
docker compose --profile search up -d --build blog-backend

# 验证后端是否已连接 ES（查看后端日志）
docker compose logs blog-backend | grep -i elasticsearch
```

#### 8.9.3 ES 启动失败 / 内存不足（OOM）

**现象**：`blog-elasticsearch` 反复重启或 `docker compose ps` 显示 `Restarting`，日志报 `OutOfMemoryError` 或 `bootstrap checks failed`（如 `max virtual memory areas vm.max_map_count` 过低）。

**解决**：

```bash
# 1. 调大宿主机 vm.max_map_count（ES 启动硬性要求，建议 ≥ 262144）
sudo sysctl -w vm.max_map_count=262144
# 持久化：echo "vm.max_map_count=262144" | sudo tee -a /etc/sysctl.conf

# 2. 确认 ES_JAVA_OPTS 内存设置合理（默认 -Xms512m -Xmx512m）
# 内存紧张时可在 docker-compose.yml 中调小，如 -Xms256m -Xmx256m
# 但需保证宿主机可用内存充足

# 3. 重启 ES
docker compose --profile search up -d --build elasticsearch
```

#### 8.9.4 ES 集群状态异常（red）

**现象**：`curl http://localhost:9200/_cluster/health` 返回 `"status":"red"`。

**解决**：

```bash
# 查看未分配的分片与原因
curl http://localhost:9200/_cluster/health?pretty
curl http://localhost:9200/_cat/shards?v

# 单节点模式下 red 多为数据卷损坏，可重置数据卷（⚠️ 会清空索引，需重新同步文章）
docker compose --profile search down -v
docker volume rm blog_es_data
docker compose --profile search up -d --build elasticsearch
# 重新发布文章以重建索引
```

---

## 9. 停止与重新部署

### 9.1 停止全部服务

```bash
cd ~/java-blog-system
docker compose down
```

> 此命令仅停止并删除容器，**不删除数据卷**，数据保留。

### 9.2 停止并清除数据（⚠️ 慎用）

```bash
# 停止并删除容器 + 数据卷（清空所有数据）
docker compose down -v
```

### 9.3 清理构建缓存

```bash
# 清理 Docker 构建缓存（解决构建异常）
docker builder prune -f

# 清理无用的镜像和容器
docker system prune -f
```

### 9.4 更新代码后重新部署

```bash
cd ~/java-blog-system

# 1. 拉取最新代码
git pull origin main

# 2. 重新构建并启动（仅重建有变更的服务）
docker compose up -d --build

# 3. 查看启动状态
docker compose ps
```

### 9.5 仅重建单个服务

```bash
# 仅重建后端
docker compose up -d --build blog-backend

# 仅重建管理后台
docker compose up -d --build blog-admin

# 仅重建前台门户
docker compose up -d --build blog-frontend
```

### 9.6 查看数据卷

```bash
# 列出数据卷
docker volume ls | grep blog

# 查看数据卷详情
docker volume inspect blog_mysql_data
docker volume inspect blog_redis_data
docker volume inspect blog_uploads_data
docker volume inspect blog_es_data
```

### 9.7 进入容器排查

```bash
# 进入后端容器
docker exec -it blog-backend bash

# 进入 MySQL
docker exec -it blog-mysql mysql -uroot -p123456

# 进入 Redis
docker exec -it blog-redis redis-cli -a 123456

# 进入 Elasticsearch（启用 search profile 后）
docker exec -it blog-elasticsearch bash

# 进入管理后台 nginx
docker exec -it blog-admin sh

# 进入前台门户 nginx
docker exec -it blog-frontend sh
```

---

## 10. （可选）监控体系部署与使用

系统内置 Actuator + Prometheus + Grafana 监控链路：后端通过 Actuator 暴露 `/api/actuator/prometheus` 指标，Prometheus 每 15 秒抓取并存储（默认保留 15 天），Grafana 预置数据源与「Java 博客系统监控」仪表盘。`prometheus` 与 `grafana` 两个服务在 `docker-compose.yml` 中通过 `profiles: ["monitor"]` 定义为**可选服务**，默认不启动。

### 10.1 配置环境变量

编辑项目根目录 `.env`，将 `monitor` 加入 `COMPOSE_PROFILES`（可与 `search` 组合），并设置 Grafana 管理员密码：

```env
COMPOSE_PROFILES=search,monitor
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=<你的强密码>   # 生产环境必须修改
```

> 未在 `.env` 中设置时，`GRAFANA_ADMIN_PASSWORD` 默认为 `admin123`，生产环境务必修改。

### 10.2 启动

```bash
cd ~/java-blog-system
docker compose up -d --build
```

启动完成后确认监控服务已运行：

```bash
docker compose ps
# 预期新增 blog-prometheus（9090）与 blog-grafana（3000）两个容器，状态为 running
```

### 10.3 验证

1. 验证后端指标端点（应返回 JSON 健康信息与 Prometheus 格式指标文本）：

```bash
# 健康检查
curl http://localhost:8080/api/actuator/health
# 预期返回 {"status":"UP",...}

# 指标端点
curl http://localhost:8080/api/actuator/prometheus
# 预期返回以 # HELP / # TYPE 开头的 Prometheus 指标文本
```

2. 浏览器访问 Prometheus Targets 页确认抓取状态：

```
http://<服务器IP>:9090/targets
```

预期 `blog-backend` 这个 Job 的 Endpoint（`http://blog-backend:8080/api/actuator/prometheus`）状态为 **UP**。

### 10.4 登录 Grafana 查看仪表盘

浏览器访问：

```
http://<服务器IP>:3000
```

使用 `.env` 中配置的账号（默认 `admin` / `admin123`）登录，进入 Dashboards → Java 博客系统监控，即可查看预置面板：当前 QPS、p95 延迟、5xx 错误率、活跃线程数、CPU 使用率、堆内存、QPS 趋势（按 URI）、JVM 堆内存、GC 暂停速率、HikariCP 连接池。

### 10.5 常见问题

#### Targets 显示 DOWN

**排查步骤**：

```bash
# 1. 确认后端容器健康（blog-backend 为 Up / healthy）
docker compose ps blog-backend

# 2. 手动验证指标端点有响应
curl http://localhost:8080/api/actuator/prometheus

# 3. 确认 prometheus 与 blog-backend 均在 blog-net 网络
docker network inspect blog-net | grep -E 'blog-prometheus|blog-backend'
```

常见原因：后端未启动完成（等待 `Started JavaBlogApplication` 后再观察 1-2 个抓取周期）；或后端/Prometheus 不在同一 `blog-net` 网络（网络异常时可 `docker compose down` 后重新 `up -d`）。

#### Grafana 忘记密码

Grafana 管理员密码仅在**首次初始化 `grafana_data` 数据卷**时生效，事后修改 `.env` 中的 `GRAFANA_ADMIN_PASSWORD` 不会更新已存在的账号。两种处理方式：

- 通过 `GF_SECURITY_ADMIN_PASSWORD` 环境变量临时覆盖后重启（适合紧急恢复）：

```bash
GRAFANA_ADMIN_PASSWORD=<新密码> docker compose up -d grafana
```

- 重置数据卷（⚠️ 会丢失自定义仪表盘等 Grafana 本地配置，预置仪表盘会重新加载）：

```bash
docker compose down
docker volume rm blog_grafana_data
# 确认 .env 中 GRAFANA_ADMIN_PASSWORD 已改为新密码后重启
docker compose up -d
```

#### 不开启 monitor 的影响

`COMPOSE_PROFILES` 不含 `monitor` 时，`prometheus` 与 `grafana` 服务不会启动，**其余所有服务照常运行**，后端业务功能完全不受影响。后端的 Actuator 指标端点仍然可用，可随时接入外部监控系统。
