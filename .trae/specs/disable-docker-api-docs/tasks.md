# Tasks
- [x] Task 1: 在 application-docker.yaml 中禁用 SpringDoc 文档端点
  - [x] SubTask 1.1: 在 `spring:` 节点下新增 `springdoc.api-docs.enabled: false`
  - [x] SubTask 1.2: 在 `spring:` 节点下新增 `springdoc.swagger-ui.enabled: false`（含中文注释说明用途）

# Task Dependencies
- 无（单文件配置修改）
