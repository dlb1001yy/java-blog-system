# ============================================================
# 环境变量模板：复制为 .env 后按需修改（.env 不会提交到 git）
#   cp .env.example .env
# Docker Compose 与后端（application*.yaml 占位）共用这些变量。
# 未设置时均使用默认值；生产环境【必须】修改带 ★ 的项。
# ============================================================

# ---- Compose 行为 ----
# 启用的服务分组（search = 附带 Elasticsearch 全文检索）
COMPOSE_PROFILES=search

# ---- 数据库（★生产必改）----
# MySQL root 密码（mysql 容器初始化 + 后端连接 + 健康检查共用）
MYSQL_ROOT_PASSWORD=123456
# 后端连接 MySQL 的账号/密码（与上保持一致）
MYSQL_USERNAME=root
MYSQL_PASSWORD=123456

# ---- Redis（★生产必改）----
REDIS_PASSWORD=123456

# ---- 安全密钥（★生产必改为强随机值）----
# JWT 签名密钥：长度 ≥ 64 字符（HS512 要求）
JWT_SECRET=ThisIsASecureSecretKeyForJwtTokenGenerationAndValidationMakeItLongEnough123
# API 请求签名密钥：需与前端构建时配置的签名密钥一致
API_SIGNING_SECRET=BlogApiSigningSecret2024!

# ---- 功能开关 ----
# 是否启用 Elasticsearch 全文检索（true 需 COMPOSE_PROFILES 包含 search）
BLOG_SEARCH_ENABLED=true

# ---- OSS 对象存储（使用 oss 存储策略时填写）----
OSS_ENDPOINT=oss-cn-hangzhou.aliyuncs.com
OSS_ACCESS_KEY_ID=your-access-key-id
OSS_ACCESS_KEY_SECRET=your-access-key-secret
OSS_BUCKET_NAME=your-bucket-name
OSS_URL_PREFIX=https://your-bucket-name.oss-cn-hangzhou.aliyuncs.com/
